services:
  - type: web
    name: neurosense-ml-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn backend_api:app --timeout 120"
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13
